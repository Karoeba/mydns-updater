services:
  mydns-updater:
    build: .
    image: mydns-updater:v1.1.0
    restart: unless-stopped
    volumes:
      - ./update.sh:/app/update.sh:ro
      - ./mydns.conf:/app/mydns.conf:ro
      - ./state:/state
    command: ["sh", "/app/update.sh"]
