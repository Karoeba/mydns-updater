services:
  test:
    image: alpine:latest
    network_mode: none
    restart: "no"
    volumes:
      - ./:/suite:ro
      - ../update.sh:/source/update.sh:ro
      - ./reports:/reports
    command: ["sh", "/suite/entrypoint.sh"]
