# 変更履歴

## v1.9.0（開発中：Docker・Synology自動復帰）

- 文書の入口をSynology・通常のDocker・Linux直接実行に分離。共通設定とログ説明は集約し、環境別の定期実行・自動復帰確認・記録の見方を整理。

- ホスト側の任意の監視を追加。進行期限超過が3回連続した場合だけ、動作中のコンテナ内部へ終了を依頼し、Dockerのunless-stoppedで復帰。
- 10分間隔・直近1時間3回まで。失敗や結果不明も数え、上限後は手動解除まで停止。履歴は永続化。
- 停止・pause・コンテナ再作成・古い起動時の依頼を検出。停止済みコンテナを起動するコマンドは使わない。
- 待機中の終了を迅速化し、待機用の子プロセスも終了・回収。
- Ubuntu向けsystemdタイマーと、Synology向けタスクスケジューラの導入手順を追加。
- 方式の7項目はDS1522+で実機確認済み。今回の定期実行・回数制限の実機確認は別途実施。

## v1.8.0（Release未公開：Linux自動復帰）

- 任意のsystemdタイマーを追加。進行期限超過が3回連続した場合だけ更新サービスの再起動を要求。
- 要求間隔は10分以上、直近1時間に3回まで。上限到達後は手動解除まで停止し、監視は継続。
- 履歴をサービス・OS再起動後も維持。正常復帰では回数を消さず、要求失敗も回数に含める。
- 手動停止を尊重し、通信・認証・設定エラーや判定失敗では再起動しない。
- Linuxの異常終了時再起動も10分待機に変更し、systemd側で1時間に4回までの起動制限を追加。
- Dockerの動作と設定は変更なし。Linuxの導入手順、模擬テスト、CIでのsystemd動作確認を追加。

## v1.7.0（Release未公開：Linux定期監視）

- systemdタイマーによる約30秒ごとの任意の定期監視を追加。
- 3回連続失敗で異常、成功で復旧を記録。正常時・継続異常時の独自ログを抑制。
- 手動停止時は監視も停止し、更新サービスを起動しない。自動再起動は含めない。
- Linuxの判定テストと、CIで実際にsystemdタイマーを動かす試験を追加。

## v1.6.0（Release未公開：ヘルスチェック）

- Docker・Synology Container Manager向けにHealthcheckを追加。
- Linux直接実行でも同じ確認コマンドを使用でき、進行記録先を環境変数で指定。
- プロセス・起動識別・進行期限を確認。追加通信と異常判定による自動再起動は行わない。
- Linuxサービスでは専用の一時ディレクトリに進行記録を保存。定期監視は未実装。

## v1.5.0（Release未公開：Docker・Linux共通化）

- MYDNS_CONFIG_DIRとMYDNS_STATE_DIRで設定・状態ディレクトリの指定に対応。Dockerの既定配置は維持。
- Linux直接実行向けの導入手順とsystemdサービス例を追加。
- Docker内とUbuntu直接実行で配置先指定の8項目を確認するテストを追加。
- Healthcheck案は別PRで保留し、この版には含めない。

## v1.4.0（Release未公開）

- 共通設定をconfig/mydns.conf、アカウント設定をconfig/accounts.confへ分割。
- 両ファイルの上書き後の再読み込み、構文チェック、ファイル別のエラー表示に対応。
- accounts.conf.exampleと旧形式からの移行手順を追加。認証情報入りの実ファイルはGit・イメージへ含めない。
- 旧形式は自動認識せず、明示的に分割して移行。state.confと既存アカウント番号を維持。
- 設定分割10項目と、アカウントファイルのホスト側上書きテストを追加。

## v1.3.0（Release未公開）

- 通信・HTTP・応答・設定・保存の失敗を分類し、発生対象と対処の目安を表示。
- DEBUG=0では初回・原因変更・段階変更・復旧を表示し、同じ失敗の繰り返しはDEBUG=1のみ表示。
- 対象ごとに連続失敗を追跡し、3回かつ10分以上で継続異常、1時間以上で長期化を表示。履歴は再起動でリセット。
- 通知成功状態の形式を保持し、Healthcheckの追加や再試行間隔の変更は行わない。

## v1.2.0（Release未公開）

- イメージ名をmydns-updater:localに変更し、配置例もバージョンを含まないmydns-updaterに統一。稼働バージョンは起動ログで確認。

- v1.1.2のデバッグ・設定再読み込み・構文チェックを取り込み。
- TZによるログのタイムゾーン設定を追加。省略・不正時はAsia/Tokyoを使用し、更新期限はUNIX時刻で管理。
- タイムゾーンの7項目を含む37項目の模擬テストと、3項目の設定再読み込みテストを実行。

## v1.1.2（Release未公開）

v1.0.0以降の変更をまとめたバージョンです。v1.1.0・v1.1.1は開発中の版で、GitHub Releasesでは公開していません。

- IPv4変更検知とアカウント別の通知成功IP・成功時刻を保存。
- CHECK_INTERVALとFORCE_UPDATE_INTERVALを分離し、設定値の検証と既定値への復帰を追加。
- 3つのIPv4取得先のフォールバックとIPv4形式検証を追加。
- state.confを永続化し、全アカウントが現在のIPに揃った時点で全体のLAST_IPV4を更新。
- 起動時にバージョンと実効設定値を表示し、DEBUGで周期ごとの確認・更新判断を表示。
- 設定をconfig/mydns.confへ移し、フォルダーマウントで上書き後の再読み込みに対応。
- アカウント設定の構造エラーを検出し、誤った設定での通知を防止。
- Dockerによる模擬テストと設定再読み込みテストをGitHub Actionsで自動化。

### Development history

- v1.1.0：IPv4変更検知、アカウント別状態、定期更新、取得先フォールバックを実装。
- v1.1.1：起動ログを追加。コンテナ名をmydns-updater、固定イメージタグを1.1.xに整理。
- v1.1.2：デバッグログ、設定フォルダーのマウント、アカウント構文チェックを追加。

## v1.0.0

- IPv4通知、複数アカウント、更新間隔設定、Docker Composeに対応。
